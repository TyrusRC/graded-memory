# Production build config (public, not a secret). Vite bakes this into the bundle.
# The Firebase-hosted frontend calls the Render backend cross-origin; without this the
# build would fall back to a relative "/api" and 404 against Firebase Hosting.
VITE_API_BASE=https://graded-memory-api.onrender.com/api
